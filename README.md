# APK 基础信息查看器（Tauri）
⚠️：纯粹的 VibeCoding 产物，仅供娱乐⚠️

没有一行手写代码，纯AI生成

## 功能

- 选择或拖拽 APK 文件
- 解析 `AndroidManifest.xml`
- 查看包名、应用名、版本、SDK 信息、权限、组件、ABI、签名文件、文件大小等
- Rust 后端直接读取 APK，无需 Android SDK

## 开发运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## 当前限制

签名还是画大饼。图标是方的，挺好。
